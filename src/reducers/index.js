import * as actions from '../actions';

const initialState = {
    navItems: ['Projects', 'Account', 'Logout', 'View Demo']
};

export const tailoredKnitsReducer = (state = initialState, action) => {
    return state;
}