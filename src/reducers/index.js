import * as actions from '../actions';

const initialState = {
    navItems: ['Projects', 'Account', 'Logout']
};

export const tailoredKnitsReducer = (state = initialState, action) => {
    return state;
}

// add action and reducer to set Nav state